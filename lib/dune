(include_subdirs qualified)

(library
  (name webremote)
  (wrapped false)
  (libraries unix tyxml ctypes.foreign audio mpris directories yojson-five))

(env
  (dev
    (flags (:standard -warn-error -A))))
