(executable
  (public_name webremote)
  (name main)
  (libraries
    webremote
    lwt
    dream
    tyxml)
  (modes byte exe)
  (preprocess
    (pps lwt_ppx)))

(env
  (dev
    (flags (:standard -warn-error -A))))

